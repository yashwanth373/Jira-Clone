import { PeopleSearchPipe } from './people-search.pipe';

describe('PeopleSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new PeopleSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
