import { IssueSearchPipe } from './issue-search.pipe';

describe('IssueSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new IssueSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
